import { createFileRoute, useNavigate } from "@tanstack/react-router";
import { FormEvent, useState } from "react";
import { supabase } from "@/integrations/supabase/client";
export const Route=createFileRoute("/auth")({component:Auth});
function friendly(e:unknown){const m=e instanceof Error?e.message:String(e);if(/already registered|already exists/i.test(m))return "Este e-mail já está cadastrado. Entre na sua conta.";if(/invalid.*email|email/i.test(m)&&/invalid|valid/i.test(m))return "Digite um e-mail válido.";if(/password/i.test(m))return "A senha precisa atender às regras configuradas no Supabase.";return m||"Não foi possível criar a conta. Tente novamente."}
function Auth(){const nav=useNavigate();const [mode,setMode]=useState<"login"|"signup">("signup"),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[name,setName]=useState(""),[loading,setLoading]=useState(false),[error,setError]=useState(""),[ok,setOk]=useState("");async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError("");setOk("");try{if(mode==="signup"){const {data,error}=await supabase.auth.signUp({email:email.trim(),password,options:{data:{full_name:name.trim()||null}}});if(error)throw error;if(!data.session){setOk("Conta criada. Confira seu e-mail para confirmar o cadastro e depois entre no app.");}else nav({to:"/"});}else{const {error}=await supabase.auth.signInWithPassword({email:email.trim(),password});if(error)throw error;nav({to:"/"});}}catch(e){setError(friendly(e))}finally{setLoading(false)}}return <main className="min-h-screen bg-slate-950 p-4 text-slate-100"><div className="mx-auto flex min-h-[90vh] max-w-md items-center"><section className="w-full rounded-3xl border border-slate-800 bg-slate-900 p-7"><div className="text-xs font-bold text-indigo-300">APROVA UNIMAR 2027</div><h1 className="mt-2 text-3xl font-black">{mode==="signup"?"Criar sua conta":"Entrar"}</h1><p className="mt-2 text-sm text-slate-400">Seu progresso, exercícios, redações e histórico ficam vinculados à sua conta.</p><form onSubmit={submit} className="mt-6 space-y-4">{mode==="signup"&&<input value={name} onChange={e=>setName(e.target.value)} placeholder="Seu nome" className="w-full rounded-xl border border-slate-700 bg-slate-950 px-4 py-3 outline-none focus:border-indigo-500"/>}<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="E-mail" className="w-full rounded-xl border border-slate-700 bg-slate-950 px-4 py-3 outline-none focus:border-indigo-500"/><input required minLength={6} type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Senha" className="w-full rounded-xl border border-slate-700 bg-slate-950 px-4 py-3 outline-none focus:border-indigo-500"/><button disabled={loading} className="w-full rounded-xl bg-indigo-500 px-4 py-3 font-bold disabled:opacity-50">{loading?"Aguarde…":mode==="signup"?"Criar conta":"Entrar"}</button></form>{error&&<div className="mt-4 rounded-xl border border-red-500/30 bg-red-500/10 p-3 text-sm text-red-200">{error}</div>}{ok&&<div className="mt-4 rounded-xl border border-emerald-500/30 bg-emerald-500/10 p-3 text-sm text-emerald-200">{ok}</div>}<button onClick={()=>{setMode(mode==="signup"?"login":"signup");setError("");setOk("")}} className="mt-5 w-full text-sm text-indigo-300">{mode==="signup"?"Já tenho conta → Entrar":"Ainda não tenho conta → Criar conta"}</button><button onClick={()=>nav({to:"/"})} className="mt-3 w-full text-xs text-slate-500">Voltar sem entrar</button></section></div></main>}
