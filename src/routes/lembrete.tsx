import { createFileRoute } from "@tanstack/react-router";
import { useMemo, useState } from "react";
import { allReminderDays, remindersForDay, TOTAL_STUDY_DAYS, totalTopics } from "@/services/complete-study-schedule";
export const Route=createFileRoute("/lembrete")({component:Lembrete});
const KEY="unimar-lembrete-concluidos-v2";
const readDone=()=>{try{return JSON.parse(localStorage.getItem(KEY)||"[]") as string[]}catch{return[]}};
function Lembrete(){const [done,setDone]=useState<string[]>(readDone),[onlyPending,setOnlyPending]=useState(false);const toggle=(id:string)=>setDone(prev=>{const next=prev.includes(id)?prev.filter(x=>x!==id):[...prev,id];localStorage.setItem(KEY,JSON.stringify(next));return next});const total=useMemo(()=>allReminderDays().reduce((n,d)=>n+remindersForDay(d).length,0),[]);const completed=done.length;return <main className="min-h-screen bg-slate-950 px-4 py-6 text-slate-100 lg:px-10"><div className="mx-auto max-w-5xl"><div className="mb-6 flex flex-wrap items-end justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-wider text-indigo-300">Lembrete de estudos</p><h1 className="mt-1 text-3xl font-black">Tudo até a prova</h1><p className="mt-2 text-sm text-slate-400">26/08/2026 → 18/10/2026 · {totalTopics()} tópicos do edital distribuídos pelos {TOTAL_STUDY_DAYS} dias.</p></div><a href="/" className="rounded-xl border border-slate-700 px-4 py-2 text-sm">Início</a></div><div className="mb-6 rounded-2xl border border-slate-800 bg-slate-900 p-4"><div className="flex flex-wrap items-center justify-between gap-3"><div><b>{completed} de {total} concluídos</b><div className="mt-2 h-2 w-72 max-w-full overflow-hidden rounded-full bg-slate-800"><div className="h-full bg-indigo-500" style={{width:`${total?Math.min(100,completed/total*100):0}%`}}/></div></div><button onClick={()=>setOnlyPending(v=>!v)} className="rounded-lg border border-slate-700 px-3 py-2 text-sm">{onlyPending?"Mostrar todos":"Mostrar pendentes"}</button></div></div><div className="space-y-5">{allReminderDays().map(day=>{const all=remindersForDay(day),tasks=all.filter(t=>!onlyPending||!done.includes(t.id));if(onlyPending&&!tasks.length)return null;const date=all[0]?.date;return <section key={day} className="rounded-2xl border border-slate-800 bg-slate-900/70 p-4"><div className="mb-3 flex items-center justify-between"><div><h2 className="font-bold">Dia {day}</h2><p className="text-xs text-slate-500">{date?.toLocaleDateString("pt-BR",{weekday:"long",day:"2-digit",month:"2-digit",year:"numeric"})}</p></div><span className="text-xs text-slate-500">{all.filter(t=>done.includes(t.id)).length}/{all.length}</span></div><div className="space-y-2">{tasks.map(t=>{const checked=done.includes(t.id);return <label key={t.id} className={`flex cursor-pointer items-start gap-3 rounded-xl border p-3 ${checked?"border-emerald-500/30 bg-emerald-500/5 opacity-60":"border-slate-800 bg-slate-950/40"}`}><input type="checkbox" checked={checked} onChange={()=>toggle(t.id)} className="mt-1 h-4 w-4"/><span className={`text-sm font-semibold ${checked?"line-through":""}`}>{t.subject} · {t.topic}{t.kind==="essay"&&<span className="ml-2 text-xs text-indigo-300">· Redação semanal</span>}</span></label>})}</div></section>})}</div></div></main>}
